# Application Settings
NODE_ENV=development        # The current environment (development, production, etc.)
PORT=5000                   # Port application will run on
# USERS_API_URL= "http://localhost:5000/authUsers"; # Backend URL for user requests

# MySQL Database Configuration
DB_HOST=localhost           # MySQL host (for local development, it's usually localhost)
DB_PORT=3306                # Default MySQL port
DB_USER=areej                # MySQL username (default root for local dev)

DB_PASSWORD=Areej12345!@

DB_NAME=skillConnectDB      # Name of the database to use in development

# Secret keys for encryption or JWT tokens
JWT_SECRET=mysecretkey      # Secret key for signing JWT token
